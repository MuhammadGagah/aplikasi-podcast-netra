import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'my.id.mgagah',
  appName: 'podcastNetra',
  webDir: 'www',
  server: {
    androidScheme: 'https',
  },
};

export default config;
